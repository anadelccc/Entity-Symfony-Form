{% extends 'base.html.twig' %}

{% block title %}Formulario{% endblock %}

{% block body %}
    <h1>Formulario</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formulario.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ formulario.Nombre }}</td>
            </tr>
            <tr>
                <th>Teléfono</th>
                <td>{{ formulario.Telefono }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_formulario_index') }}">Vuelve</a>

    <a href="{{ path('app_formulario_edit', {'id': formulario.id}) }}">edit</a>

    {{ include('formulario/_delete_form.html.twig') }}
{% endblock %}
